<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %>
    <style>
        .game-card {
            transition: all 0.3s ease;
            border-radius: 15px;
            overflow: hidden;
            border: none;
            box-shadow: 0 4px 15px rgba(0,0,0,0.05);
        }
        .game-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.1);
        }
        .status-badge {
            font-size: 0.75rem;
            padding: 5px 12px;
            border-radius: 20px;
        }
        .declare-btn {
            border-radius: 10px;
            font-weight: 600;
            padding: 8px 15px;
        }
        .winning-num-input {
            font-size: 1.5rem;
            text-align: center;
            font-weight: bold;
            letter-spacing: 5px;
            border-radius: 10px;
        }
        .text-purple-gradient {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        .text-purple {
            color: #667eea;
        }
        .btn-purple-gradient {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white !important;
            border: none;
            transition: all 0.3s;
        }
        .btn-purple-gradient:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }
    </style>
</head>
<body>

    <div class="wrapper d-flex">
        <!-- Sidebar -->
        <%- include('partials/sidebar') %>

        <!-- Page Content -->
        <div id="content">
            <!-- Top Navbar -->
            <%- include('partials/header') %>

            <div class="container-fluid px-4">
                <div class="row mb-4">
                    <div class="col-md-8">
                        <h5 class="fw-bold text-dark">Today's Games (<%= today %>)</h5>
                        <p class="text-secondary small">Enter winning numbers and process payouts for today's sessions.</p>
                    </div>
                </div>

                <div class="row g-4">
                    <% games.forEach(game => { %>
                        <div class="col-xl-4 col-md-6">
                            <div class="card game-card h-100">
                                <div class="card-body p-4">
                                    <div class="d-flex justify-content-between align-items-start mb-3">
                                        <div>
                                            <h5 class="fw-bold mb-1"><%= game.name %></h5>
                                            <div class="small text-muted">
                                                <i class="far fa-clock me-1"></i> <%= game.open_time %> - <%= game.close_time %>
                                            </div>
                                        </div>
                                        <span class="status-badge <%= game.session_status === 'completed' ? 'bg-success text-white' : 'bg-warning text-dark' %>">
                                            <%= game.session_status ? game.session_status.toUpperCase() : 'PENDING' %>
                                        </span>
                                    </div>

                                    <hr class="my-3 opacity-25">

                                    <% if (game.session_status === 'completed') { %>
                                        <div class="text-center py-2" id="display-<%= game.session_id %>">
                                            <div class="small text-muted mb-1">Declared Winning Number</div>
                                            <div class="display-6 fw-bold text-primary"><%= game.winning_number %></div>
                                            <div class="small text-muted mt-2">
                                                <i class="fas fa-check-circle text-success me-1"></i> Payouts Processed
                                            </div>
                                            <button class="btn btn-sm btn-outline-secondary mt-3" onclick="enableEdit('<%= game.session_id %>')">
                                                <i class="fas fa-edit me-1"></i> Change Result
                                            </button>
                                        </div>

                                        <div class="d-none" id="edit-<%= game.session_id %>">
                                            <div class="mb-3">
                                                <label class="form-label small fw-bold text-danger">New Winning Number</label>
                                                <input type="text" id="edit-input-<%= game.session_id %>" class="form-control winning-num-input" maxlength="2" placeholder="--" value="<%= game.winning_number %>">
                                            </div>
                                            <div class="d-grid gap-2">
                                                <button id="btn-save-<%= game.session_id %>" class="btn btn-danger declare-btn" onclick="saveEdit('<%= game.session_id %>', '<%= game.name %>')">
                                                    <i class="fas fa-save me-1"></i> Save Correction
                                                </button>
                                                <button class="btn btn-secondary declare-btn btn-sm" onclick="cancelEdit('<%= game.session_id %>')">
                                                    Cancel
                                                </button>
                                            </div>
                                            <div class="small text-danger mt-2 fst-italic text-center" style="font-size: 0.75rem;">
                                                Warning: Reverses previous payouts!
                                            </div>
                                        </div>
                                    <% } else if (game.is_scheduled) { %>
                                        <div class="text-center py-2">
                                            <div class="small text-muted mb-1">Scheduled Winning Number</div>
                                            <div class="display-6 fw-bold text-purple-gradient"><%= game.scheduled_winning_number %></div>
                                            <div class="small text-muted mt-2">
                                                <i class="fas fa-clock text-purple me-1"></i> Will declare at <%= game.close_time %>
                                            </div>
                                        </div>
                                    <% } else { %>
                                        <div class="mb-3">
                                            <label class="form-label small fw-bold">Enter Winning Number (00-99)</label>
                                            <input type="text" id="win-<%= game.session_id %>" class="form-control winning-num-input" maxlength="2" placeholder="--">
                                        </div>
                                        <div class="d-grid gap-2">
                                            <button id="btn-declare-<%= game.session_id %>" class="btn btn-primary declare-btn" onclick="declareResult('<%= game.session_id %>', '<%= game.name %>')">
                                                <i class="fas fa-check-circle me-1"></i> Declare Result
                                            </button>
                                            <button id="btn-schedule-<%= game.session_id %>" class="btn btn-purple-gradient declare-btn btn-sm" onclick="scheduleResult('<%= game.session_id %>', '<%= game.name %>')">
                                                <i class="fas fa-clock me-1"></i> Schedule Result
                                            </button>
                                        </div>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <%- include('partials/scripts') %>

    <script>
        async function declareResult(sessionId, gameName) {
            const winningNumber = document.getElementById(`win-${sessionId}`).value;
            const btnDeclare = document.getElementById(`btn-declare-${sessionId}`);
            const btnSchedule = document.getElementById(`btn-schedule-${sessionId}`);
            const inputField = document.getElementById(`win-${sessionId}`);
            
            if (!winningNumber || winningNumber.length !== 2) {
                alert('Please enter a valid 2-digit winning number');
                return;
            }

            if (!confirm(`Are you sure you want to declare ${winningNumber} as the winning number for ${gameName}? This action is irreversible and will process all payouts!`)) {
                return;
            }

            // Disable UI
            if(btnDeclare) btnDeclare.disabled = true;
            if(btnSchedule) btnSchedule.disabled = true;
            if(inputField) inputField.disabled = true;
            if(btnDeclare) btnDeclare.innerHTML = '<i class="fas fa-spinner fa-spin me-1"></i> Declaring...';

            try {
                const response = await fetch('/admin/api/results/declare', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ sessionId, winningNumber })
                });

                const result = await response.json();
                if (result.success) {
                    alert(result.message || 'Result declared successfully!');
                    window.location.reload();
                } else {
                    alert('Error: ' + result.message);
                    // Re-enable UI
                    if(btnDeclare) btnDeclare.disabled = false;
                    if(btnSchedule) btnSchedule.disabled = false;
                    if(inputField) inputField.disabled = false;
                    if(btnDeclare) btnDeclare.innerHTML = '<i class="fas fa-check-circle me-1"></i> Declare Result';
                    
                }
            } catch (error) {
                console.error('Error:', error);
                alert('An error occurred while declaring result');
                // Re-enable UI
                if(btnDeclare) btnDeclare.disabled = false;
                if(btnSchedule) btnSchedule.disabled = false;
                if(inputField) inputField.disabled = false;
                if(btnDeclare) btnDeclare.innerHTML = '<i class="fas fa-check-circle me-1"></i> Declare Result';
            }
        }

        async function scheduleResult(sessionId, gameName) {
            const winningNumber = document.getElementById(`win-${sessionId}`).value;
            const btnDeclare = document.getElementById(`btn-declare-${sessionId}`);
            const btnSchedule = document.getElementById(`btn-schedule-${sessionId}`);
            const inputField = document.getElementById(`win-${sessionId}`);
            
            if (!winningNumber || winningNumber.length !== 2) {
                alert('Please enter a valid 2-digit winning number');
                return;
            }

            if (!confirm(`Schedule ${winningNumber} for ${gameName}? It will be automatically declared at the game's close time.`)) {
                return;
            }

             // Disable UI
             if(btnDeclare) btnDeclare.disabled = true;
             if(btnSchedule) btnSchedule.disabled = true;
             if(inputField) inputField.disabled = true;
             if(btnSchedule) btnSchedule.innerHTML = '<i class="fas fa-spinner fa-spin me-1"></i> Scheduling...';

            try {
                const response = await fetch('/admin/api/results/schedule', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ sessionId, winningNumber })
                });

                const result = await response.json();
                if (result.success) {
                    // Update UI to show scheduled state specifically (or just reload)
                     if(btnSchedule) btnSchedule.innerHTML = '<i class="fas fa-check me-1"></i> Scheduled';
                    alert(result.message || 'Result scheduled successfully!');
                    window.location.reload();
                } else {
                    alert('Error: ' + result.message);
                     // Re-enable UI
                    if(btnDeclare) btnDeclare.disabled = false;
                    if(btnSchedule) btnSchedule.disabled = false;
                    if(inputField) inputField.disabled = false;
                    if(btnSchedule) btnSchedule.innerHTML = '<i class="fas fa-clock me-1"></i> Schedule Result';
                }
            } catch (error) {
                console.error('Error:', error);
                alert('An error occurred while scheduling result');
                 // Re-enable UI
                 if(btnDeclare) btnDeclare.disabled = false;
                 if(btnSchedule) btnSchedule.disabled = false;
                 if(inputField) inputField.disabled = false;
                 if(btnSchedule) btnSchedule.innerHTML = '<i class="fas fa-clock me-1"></i> Schedule Result';
            }
        }

        function enableEdit(sessionId) {
            document.getElementById(`display-${sessionId}`).classList.add('d-none');
            document.getElementById(`edit-${sessionId}`).classList.remove('d-none');
        }

        function cancelEdit(sessionId) {
            document.getElementById(`display-${sessionId}`).classList.remove('d-none');
            document.getElementById(`edit-${sessionId}`).classList.add('d-none');
        }

        async function saveEdit(sessionId, gameName) {
            const winningNumber = document.getElementById(`edit-input-${sessionId}`).value;
            const btnSave = document.getElementById(`btn-save-${sessionId}`);
            const inputField = document.getElementById(`edit-input-${sessionId}`);
            
            if (!winningNumber || winningNumber.length !== 2) {
                alert('Please enter a valid 2-digit number');
                return;
            }

            if (!confirm(`CRITICAL WARNING: Are you sure you want to change the result for ${gameName} to ${winningNumber}?\n\nThis will:\n1. DEDUCT winning amounts from users who won previously.\n2. ADD winning amounts to new winners.\n3. Update all bet statuses.\n\nThis action cannot be cleanly undone if repeated multiple times.`)) {
                return;
            }

             // Disable UI
             if(btnSave) {
                 btnSave.disabled = true;
                 btnSave.innerHTML = '<i class="fas fa-spinner fa-spin me-1"></i> Processing...';
             }
             if(inputField) inputField.disabled = true;

            try {
                const response = await fetch('/admin/api/results/edit', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ sessionId, winningNumber })
                });

                const result = await response.json();
                if (result.success) {
                    alert(result.message || 'Result corrected successfully!');
                    window.location.reload();
                } else {
                    alert('Error: ' + result.message);
                    // Re-enable UI
                    if(btnSave) {
                        btnSave.disabled = false;
                        btnSave.innerHTML = '<i class="fas fa-save me-1"></i> Save Correction';
                    }
                    if(inputField) inputField.disabled = false;
                }
            } catch (error) {
                console.error('Error:', error);
                alert('An error occurred while correcting result');
                 // Re-enable UI
                 if(btnSave) {
                    btnSave.disabled = false;
                    btnSave.innerHTML = '<i class="fas fa-save me-1"></i> Save Correction';
                }
                if(inputField) inputField.disabled = false;
            }
        }

    </script>
</body>
</html>
